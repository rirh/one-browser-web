import type { MessageCatalog } from './types';

// Chinese is the current source language of the desktop app. Source strings
// without an explicit override intentionally render unchanged.
export const zhCN = {} satisfies MessageCatalog;
